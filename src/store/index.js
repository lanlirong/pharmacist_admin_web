import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

export default new Vuex.Store({
    modules: {},
    state: {
        pageSize: 20 // 系统默认的分页大小
    },
    mutations: {},
    actions: {}
});
